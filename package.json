{
  "name": "sweatcheck-dm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "start": "npm start --prefix client",
    "heroku-postbuild": "npm run install:all && npm run build --prefix client",
    "test": "npm test --prefix client",
    "eject": "npm run eject --prefix client",
    "build": "cd client && npm run build"
  },
  "author": "Chris Papanicholas",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.3.1",
    "npm-run-all": "^4.1.5",
    "react-router-dom": "^6.21.1"
  }
}
